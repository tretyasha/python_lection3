import modul1

print(modul1.max(5, 9))

#давайте еще раз повторим,чтобы вызвать какую-то функцию из другого файлика из другого модуля мы должны импортировать этот модуль.
#затем обратиться к этому модулю и указать название нашей функции.и соответственно передаем какие-то значения.
# можем сделать по-другому
# сразу напрямую импортировать нашу функцию